{
"size": 500,
"query": {
    "bool": {
      "must": [{ "match": { "ip": "127.0.0.1" } }],
      "filter": [
            {"range": { "time": { "gte": "now-24h", "lte": "now", "format": "epoch_millis" } } }
      ]
    }
  },
 "_source": ["ip","time","port","script","script_output"]
}
